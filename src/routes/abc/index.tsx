import { component$ } from "@builder.io/qwik";
import { routeLoader$ } from "@builder.io/qwik-city";
import { productStream } from "~/components/utils/data";




export default component$(() => {
   
    return (
        <div>
        <h1>ABC</h1>
        </div>
    );
});